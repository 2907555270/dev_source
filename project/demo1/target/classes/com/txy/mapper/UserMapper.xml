<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.txy.mapper.UserMapper">


    <select id="findAll" resultType="user">
        select * from tb_user
    </select>

    <select id="findByConditions" resultType="user">
        select * from demo1.tb_user
        <where>
            <if test="name!=null and name!=''">
                and name like concat('%',#{name},'%')
            </if>
            <if test="username!=null and username!=''">
                and username like concat('%',#{username},'%')
            </if>
            <if test="age!=null">
                and age = #{age}
            </if>
            <if test="email!=null and email!=''">
                and email like concat('%',#{email},'%')
            </if>
        </where>
    </select>

    <select id="findByUsername" resultType="user">
        select * from tb_user where username=#{username}
    </select>

    <select id="findByUsernameAndPassword" resultType="user" parameterType="user">
        select * from tb_user where username=#{username} and password=#{password}
    </select>

    <insert id="insert" parameterType="user">
        insert into demo1.tb_user
        values (#{uid},
                #{username},
                #{password},
                #{name},
                #{age},
                #{address},
                #{phone},
                #{email},
                #{head_img})
    </insert>

    <update id="updatePassword" parameterType="user">
        update demo1.tb_user set password = #{password} where username=#{username} and email=#{email};
    </update>

    <update id="update" parameterType="user">
        update demo1.tb_user
        <set>
            <if test="password!=null">
                password = #{password},
            </if>
            <if test="name!=null">
                name = #{name},
            </if>
            <if test="age!=0">
                age = #{age},
            </if>
            <if test="address!=null">
                address = #{address},
            </if>
            <if test="phone!=null">
                phone = #{phone},
            </if>
            <if test="email!=null">
                email = #{email},
            </if>
            <if test="head_img!=null">
                head_img = #{head_img},
            </if>
        </set>
        where username = #{username}
    </update>

    <delete id="delete" parameterType="String">
        delete from tb_user where username=#{username}
    </delete>
</mapper>