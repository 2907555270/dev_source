<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.txy.mapper.AdminMapper">
    <select id="findByUsernameAndPassword" parameterType="admin" resultType="admin">
        select * from tb_admin where username=#{username} and password=#{password}
    </select>

    <update id="update" parameterType="admin">
        update tb_admin set password=#{password} where username=#{username}
    </update>
</mapper>